# Flow: 봇 관리

봇의 생성, 시작, 중지, 삭제 및 상세 정보 확인 흐름.

## Seed Data

- **봇 A** (created): bot_id="bot-sma-01", name="SMA 크로스 봇", strategy_name="sma_cross", status=created, mode=paper
  - interval_seconds=60, symbols=["005930"], allocated_budget=1000000
  - 로그 없음
- **봇 B** (running): bot_id="bot-momentum-01", name="모멘텀 추세 봇", strategy_name="momentum_v2", status=running, mode=live
  - interval_seconds=300, symbols=["005930", "000660"], allocated_budget=5000000
  - 로그: 성공 3건, 실패 1건
- **봇 C** (stopped): bot_id="bot-mean-01", name="평균회귀 실전 봇", strategy_name="mean_revert", status=stopped, mode=live
  - interval_seconds=120, symbols=["035420"], allocated_budget=3000000
  - 로그: 성공 5건
- **봇 D** (error): bot_id="bot-err-01", name="오류 테스트 봇", strategy_name="broken_strategy", status=error, mode=paper
  - interval_seconds=60, symbols=[], allocated_budget=500000
  - 로그: 실패 2건 (에러 메시지 포함)

---

## 1. 봇 목록 페이지 진입

1. 사이드바에서 "🤖 봇 관리" 클릭
2. **Expected**: `/bots` 페이지로 이동
3. **Expected**: 헤더에 "봇 관리" 타이틀 표시
4. **Expected**: "봇 생성" 버튼 표시 (파란색)
5. **Expected**: 2개 섹션으로 구분 — "실행 중", "비활성"

## 2. 실행 중 섹션

1. "실행 중" 섹션 확인
2. **Expected**: 섹션 타이틀 "실행 중" + 건수 뱃지 "1" 표시
3. **Expected**: 봇 B 카드 표시
   - 이름: "모멘텀 추세 봇"
   - ID: "bot-momentum-01" (링크)
   - 상태 뱃지: "실행중" (초록색/positive)
   - 전략: "momentum_v2" (링크)
   - 모드: "실전" 뱃지
   - 실행 간격: "300초"
   - 액션: "중지" 버튼

## 3. 비활성 섹션

1. "비활성" 섹션 확인
2. **Expected**: 섹션 타이틀 "비활성" + 건수 뱃지 "3" 표시
3. **Expected**: 봇 A 카드
   - 이름: "SMA 크로스 봇"
   - 상태 뱃지: "생성됨" (회색/muted)
   - 모드: "모의" 뱃지
   - 액션: "시작", "삭제" 버튼
4. **Expected**: 봇 C 카드
   - 이름: "평균회귀 실전 봇"
   - 상태 뱃지: "중지됨" (회색/muted)
   - 모드: "실전" 뱃지
   - 액션: "시작", "삭제" 버튼
5. **Expected**: 봇 D 카드
   - 이름: "오류 테스트 봇"
   - 상태 뱃지: "오류" (빨간색/negative)
   - 액션: "삭제" 버튼

## 4. 봇 생성 — 모달 열기

1. "봇 생성" 버튼 클릭
2. **Expected**: 생성 모달 표시
3. **Expected**: "봇 생성" 모달 타이틀 표시
4. **Expected**: 폼 필드 7개 — 이름, 봇 ID, 전략, 봇 유형, 실행 간격, 예산, 대상 종목
5. **Expected**: 봇 유형의 "모의투자" 버튼이 기본 선택 (파란색)
6. **Expected**: 실행 간격 기본값 "60"
7. **Expected**: "취소", "생성" 버튼 표시

## 5. 봇 생성 — 유형 토글

1. "실전투자" 버튼 클릭
2. **Expected**: "실전투자" 버튼이 주황색 배경으로 변경
3. **Expected**: "모의투자" 버튼이 비활성 스타일로 변경
4. "모의투자" 버튼 클릭
5. **Expected**: 원래 상태로 복원

## 6. 봇 생성 — 폼 작성 및 제출

1. 이름: "RSI 반등 봇" 입력
2. 봇 ID: "bot-rsi-01" 입력
3. 전략: "rsi_reversal" 입력
4. 봇 유형: "모의투자" 선택
5. 실행 간격: "120" 입력
6. 예산: "2000000" 입력
7. 대상 종목: "005930, 035420" 입력
8. "생성" 버튼 클릭
9. **Expected**: 모달이 닫힘
10. **Expected**: 비활성 섹션에 "RSI 반등 봇" 카드가 추가 (상태: 생성됨)

## 7. 봇 생성 — 취소

1. "봇 생성" 버튼 클릭 → 모달 열기
2. 이름: "임시 봇" 입력
3. "취소" 버튼 클릭
4. **Expected**: 모달이 닫힘
5. **Expected**: 봇 목록에 "임시 봇"이 추가되지 않음

## 8. 봇 시작 — 카드에서

1. 봇 A 카드의 "시작" 버튼 클릭
2. **Expected**: 봇 A가 비활성 섹션에서 실행 중 섹션으로 이동
3. **Expected**: 상태 뱃지가 "실행중" (초록색)으로 변경
4. **Expected**: 액션 버튼이 "중지"로 변경

## 9. 봇 중지 — 카드에서

1. 봇 B 카드의 "중지" 버튼 클릭
2. **Expected**: 봇 B가 실행 중 섹션에서 비활성 섹션으로 이동
3. **Expected**: 상태 뱃지가 "중지됨" (회색)으로 변경
4. **Expected**: 액션 버튼이 "시작", "삭제"로 변경

## 10. 봇 삭제 — 확인 및 취소

1. 봇 D 카드의 "삭제" 버튼 클릭
2. **Expected**: 삭제 확인 대화상자 표시
3. 취소 선택
4. **Expected**: 봇 D 카드가 그대로 존재

## 11. 봇 삭제 — 확인

1. 봇 D 카드의 "삭제" 버튼 클릭
2. 확인 선택
3. **Expected**: 봇 D 카드가 사라짐
4. **Expected**: 비활성 섹션 건수 뱃지 감소

## 12. 봇 상세 — created 봇 (봇 A)

1. 봇 A 카드의 ID 링크 "bot-sma-01" 클릭
2. **Expected**: `/bots/bot-sma-01` 상세 페이지로 이동
3. **Expected**: "SMA 크로스 봇" 타이틀 표시, 하위에 "bot-sma-01" ID 표시
4. **Expected**: 상태 뱃지 "생성됨", 유형 뱃지 "모의" 표시
5. **Expected**: 전략 "sma_cross" 링크 표시
6. **Expected**: 헤더 우측에 "시작" 버튼 표시

## 13. 봇 상세 — 실행 설정

1. "실행 설정" 섹션 확인
2. **Expected**: 3개 항목 표시
   - 실행 간격: "60초"
   - 대상 종목: "005930"
   - 할당 예산: "1,000,000원"

## 14. 봇 상세 — 실행 로그 (빈 상태)

1. "실행 로그" 섹션 확인
2. **Expected**: "로그가 없습니다" 메시지 표시

## 15. 봇 상세 — running 봇 (봇 B)

1. 뒤로가기 → 봇 B 카드의 ID 링크 클릭
2. **Expected**: "모멘텀 추세 봇" 타이틀, 상태 "실행 중" (초록색) 표시
3. **Expected**: 유형 뱃지 "실전" (주황색/warning) 표시
4. **Expected**: 헤더 우측에 "중지" 버튼 표시 ("시작" 버튼 미표시)
5. **Expected**: 실행 설정
   - 실행 간격: "300초"
   - 대상 종목: "005930, 000660"
   - 할당 예산: "5,000,000원"

## 16. 봇 상세 — 실행 로그 (데이터 있음)

1. "실행 로그" 섹션 확인
2. **Expected**: 4건의 로그 표시
3. **Expected**: 각 행에 타임스탬프, 성공/실패 뱃지, 메시지 표시
4. **Expected**: 성공 → 초록색 "성공" 뱃지, 실패 → 빨간색 "실패" 뱃지

## 17. 봇 상세 — stopped 봇 (봇 C)

1. 뒤로가기 → 봇 C 카드의 ID 링크 클릭
2. **Expected**: "평균회귀 실전 봇" 타이틀, 상태 "중지됨" 표시
3. **Expected**: 유형 뱃지 "실전" 표시
4. **Expected**: 헤더 우측에 "시작" 버튼 표시

## 18. 봇 상세 — error 봇 (봇 D)

1. 뒤로가기 → 봇 D 카드의 ID 링크 클릭
2. **Expected**: "오류 테스트 봇" 타이틀, 상태 "오류" (빨간색) 표시
3. **Expected**: 실행 로그에 실패 2건 표시, 에러 메시지 포함

## 19. 봇 상세에서 시작/중지

1. 봇 C 상세 페이지에서 "시작" 버튼 클릭
2. **Expected**: 상태 뱃지가 "실행 중"으로 변경
3. **Expected**: "시작" 버튼이 "중지" 버튼으로 교체
4. "중지" 버튼 클릭
5. **Expected**: 상태 뱃지가 "중지됨"으로 변경
6. **Expected**: "중지" 버튼이 "시작" 버튼으로 교체
