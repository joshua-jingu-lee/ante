services:
  ante:
    build: .
    ports:
      - "3982:3982"
    volumes:
      - ./config:/app/config
      - ante-data:/app/data
      - ante-db:/app/db
    env_file:
      - config/secrets.env

volumes:
  ante-data:
  ante-db:
